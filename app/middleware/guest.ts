/**
 * guest — STUB tạm cho giai đoạn design (chưa apply API).
 * Bản gốc chặn người đã đăng nhập vào lại trang auth. Hiện cho qua tất cả;
 * guard thật khôi phục ở plan migrate API.
 */
export default defineNuxtRouteMiddleware(() => {
  // no-op (auth tạm tắt ở giai đoạn design)
})
