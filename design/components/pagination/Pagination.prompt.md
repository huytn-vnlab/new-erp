Sits in a table/card footer. `page` is 1-based; parent owns the state.

```jsx
<Pagination page={page} total={248} perPage={10} onChange={setPage} />
```

Renders up to 5 numbered pages then an ellipsis. Vietnamese labels: Trước / Sau.
